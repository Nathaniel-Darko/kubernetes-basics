apiVersion: v1
kind: Service
metadata:
  name: k8s-challenge-service
  labels:
    app: k8s-challenge
spec:
  type: NodePort
  selector:
    app: k8s-challenge
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080
      protocol: TCP